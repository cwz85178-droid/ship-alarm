# 船舱报警灯 — 梯形图(LAD)网络说明 & 博图导入指南

## 一、博图 V16 项目创建步骤

### 1. 新建项目
```
创建新项目 → 项目名称: ShipCabinAlarm → 路径: 自选 → 创建
```

### 2. 添加 CPU
```
添加新设备 → 控制器 → SIMATIC S7-1200 → CPU → 
CPU 1214C DC/DC/DC → 订货号 6ES7 214-1AG40-0XB0 → 确定
```

### 3. 启用时钟存储器（关键！）
```
项目树 → PLC_1 → 设备配置 → 双击 CPU → 
属性 → 系统与时钟存储器 → 
☑ 启用时钟存储器字节 → 地址保持 MB0 → 确定
```

### 4. 导入 OB1 源码
```
项目树 → PLC_1 → 程序块 → 添加新块 →
组织块(OB) → 名称 OB1 → 语言 SCL → 确定
→ 打开 OB1 → 全选删除 → 粘贴 OB1_Main.scl 内容 → 编译
```

编译时系统会自动创建三个背景数据块：
- `R_TRIG_DB` — 上升沿检测实例
- `SR_DB` — SR 触发器实例
- `IEC_TON_DB` — IEC 定时器实例

---

## 二、梯形图逐网络说明

### 网络1：启动按钮上升沿检测

```
  I0.0       R_TRIG        M10.1
--| |--------[ P ]---------(  )--
 启动按钮                  上升沿脉冲
 常开触点
```

**原理：** 当 I0.0 从 0→1 时，M10.1 只在当前扫描周期为 1，下一个周期恢复为 0。这样可以捕捉按钮的"按下动作"，而不是"按着不放的状态"。

**为什么用上升沿：** 灯2 是"按一下→闪5秒→灭"，需要的是"按下的那一刻"作为触发信号，而不是按钮本身的状态。

---

### 网络2：SR 触发器 — 定时器运行标志

```
  M10.1              M10.2
--| |----[ S     SR ]--(  )--
 上升沿    [       ]
  M10.3    [   Q   ]
--| |-----[ R     ]
 定时到
```

**原理：** 
- M10.1 上升沿 → **S(置位)** → M10.2 锁定为 1
- M10.3 定时到 → **R(复位)** → M10.2 解锁为 0
- M10.2 = 1 期间，代表"定时器正在跑"

**为什么用 SR 而不是自锁：** 定时到需要"强制解除"锁存，SR 是最干净的方式。

---

### 网络3：IEC 接通延时定时器 TON（5秒）

```
  M10.2         TON           M10.3
--| |-----[IN  T#5s  Q]------(  )--
 定时使能     IEC定时器       定时到
```

**原理：** 
- IN = 1（M10.2=1）：定时器开始累加时间
- 累加到 5s 后，Q 输出 M10.3=1
- M10.3=1 → 反馈到网络2 复位 SR → M10.2=0 → 定时器停止

**时序图：**

```
I0.0   ___|~~~~|__________________|~~~~|___________
               |                  |
M10.1  ________|~|________________|~|_____________
               | 上升沿            | 上升沿
M10.2  ________|+++++++++++_______|+++++++++++____
               |  锁存             |  锁存
M10.3  ________|___________|~|____|___________|~|__
               |           |5s到   |           |5s到
Q0.1   ________|~|~|~|~|~||______|~|~|~|~|~||____
               | 灯2闪烁   |灭     | 灯2闪烁   |灭
                        ← 5s →           ← 5s →
```

---

### 网络4：报警灯1 — 按下闪烁，松开即灭

```
  I0.0       M0.5              Q0.0
--| |--------| |------[ ]------(  )--
 启动按钮   1Hz时钟   (连线)    灯1
```

**原理：** 
- 按着按钮（I0.0=1）**且** 1Hz 脉冲（500ms ON / 500ms OFF）→ 灯1 闪烁
- 松开按钮（I0.0=0）→ 灯1 立即灭
- M0.5 来自 CPU 硬件时钟，不需要任何代码

**1Hz 时钟波形：**
```
M0.5    ___|~~~|___|~~~|___|~~~|___|~~~|___
        0.5s  0.5s
```

---

### 网络5：报警灯2 — 触发后闪烁5秒自动熄灭

```
  M10.2     M10.3     M0.5              Q0.1
--| |-------|/|--------| |------[ ]-----(  )--
 定时运行  未到5s    1Hz时钟  (连线)    灯2
```

**原理：**
- M10.2=1（定时器正在跑）**且** M10.3=0（还没到5秒）**且** 1Hz 脉冲 → 灯2 闪烁
- M10.3=1（5秒到了）→ M10.2 被网络2复位 → 灯2 灭
- 如果再次按下按钮 → 新的上升沿 → 网络1→2→3 重新开始 → 灯2 再次闪烁5秒

---

## 三、使用 LAD 语言手动创建的方法

如果你更想在博图里直接画梯形图（不用 SCL 导入），按以下步骤：

### 步骤1：创建 OB1（LAD 语言）
```
添加新块 → OB → 名称 OB1 → 语言 LAD → 确定
```

### 步骤2：添加上升沿指令
```
右侧指令栏 → 基本指令 → 位逻辑 → 拖入"扫描操作数信号上升沿" → 
输入 CLK: I0.0, Q: M10.1, M_BIT: M10.0
```

### 步骤3：添加 SR 触发器
```
基本指令 → 位逻辑 → 拖入"SR 置位复位触发器" →
S: M10.1, R: M10.3, Q: M10.2
```

### 步骤4：添加 TON 定时器
```
右侧指令栏 → 基本指令 → 定时器操作 → 拖入"TON 接通延时" →
IN: M10.2, PT: T#5s, Q: M10.3
（系统自动创建背景 DB）
```

### 步骤5：添加灯1 和 灯2 输出
```
网络4：常开触点 I0.0 — 常开触点 M0.5 — 线圈 Q0.0
网络5：常开触点 M10.2 — 常闭触点 M10.3 — 常开触点 M0.5 — 线圈 Q0.1
```

---

## 四、完整变量符号表

| 符号名 | 地址 | 数据类型 | 注释 |
|--------|------|----------|------|
| Start_Button | %I0.0 | Bool | 启动按钮（NO 常开） |
| Alarm_Light_1 | %Q0.0 | Bool | 报警灯1 |
| Alarm_Light_2 | %Q0.1 | Bool | 报警灯2 |
| Clock_1Hz | %M0.5 | Bool | 1Hz 时钟脉冲 |
| Edge_Mem_Bit | %M10.0 | Bool | 上升沿记忆位 |
| Button_Rise | %M10.1 | Bool | 按钮上升沿脉冲 |
| Timer_Run | %M10.2 | Bool | 定时器运行标志 |
| Timer_Done | %M10.3 | Bool | 5秒定时到 |
